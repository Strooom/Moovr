#include "machineproperties.h"
#include "version.h"
#include "eventbuffer.h"
#include "maincontroller.h"
#include "motionctrl.h"

int main(int argc, char **argv) {
    // does nothing for the time being, only to make sure the build finds and uses it
}